#ifndef IMAGE_H
#define IMAGE_H

#include "../glad/glad.h"
#include "../video/video.h"

////////////////////////////////////////////////////////////////////////////////////////////////////////////
//Structs
typedef struct image_struct
{
	unsigned int 	shader,texture,VAO,VBO;
	int 			width,height,channels,psu_loc;
	float			aspectRatio;
} image_struct;
////////////////////////////////////////////////////////////////////////////////////////////////////////////


////////////////////////////////////////////////////////////////////////////////////////////////////////////
//Functions
int init_image();
void load_image(char*);
void unload_image();
void draw_image(float,float,float,float);
void write_image(int,int,void*);
void update_imagevp();
void reset_image();
void destroy_image();
////////////////////////////////////////////////////////////////////////////////////////////////////////////

#endif //IMAGE_H